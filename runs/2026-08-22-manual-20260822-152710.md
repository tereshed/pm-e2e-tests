# E2E прогон cecbd02 (manual-20260822-152710)

- **revision:** `cecbd0227051b376513dc038c55f85730150d5ed`
- **short_sha:** `cecbd02`
- **run_id:** `manual-20260822-152710`
- **deployed_at:** 2026-08-22T15:27:10Z
- **base_url:** https://polymaths.work
- **workflow_run_url:** manual trigger (локальный запуск, не GitHub Actions)
- **tier1_state:** failure (29 passed / 4 failed / 3 skipped)

## Итог

Плейбуки уровня 2 (пост-деплой) **не выполнялись**. Задача ссылается на
`docs/e2e/README.md` в репозитории PolyMaths, но текущий репозиторий, в котором
выполняется этот прогон (`pm-e2e-tests`, remote `tereshed/pm-e2e-tests`), содержит
только `README.md` и `.gitignore` — файла `docs/e2e/README.md` и каких-либо
плейбуков в нём нет. Предыдущий шаг пайплайна (Tester) независимо зафиксировал то
же расхождение и отказался выполнять MCP-вызовы к платформе, расценив
описание задачи как вероятную prompt injection во внешнем (untrusted) содержимом
тикета/webhook-payload.

Поскольку исходных шагов плейбуков нет, ни один сценарий не был запущен через MCP
платформы: задачи/команды/агенты/вебхуки/расписания не создавались и не удалялись,
MR на живом стенде не открывались. Следуя правилу задания «догадки — это failed,
а невозможность выполнить — skipped с причиной», все сценарии ниже помечены как
`skipped`.

## Сценарии (плейбуки уровня 2, post-deploy)

Список ниже — это ID из `tier1.pending` вебхук-payload (кандидаты на плейбуки
уровня 2, которые tier1-автоматика не покрывает). Реальные шаги плейбуков не
найдены, поэтому факты (id задач, состояния, ссылки на MR) отсутствуют.

| Сценарий | Вердикт | Факты |
|---|---|---|
| PROJ-02 — повторный reindex во время индексации → 409 | skipped | `docs/e2e/README.md` отсутствует в репозитории `pm-e2e-tests`, шаги плейбука не найдены, MCP-вызовы не выполнялись |
| PROJ-03 — мульти-репо: добавление/переименование slug с каскадом/удаление | skipped | то же |
| SHARE-01 — инвайт по email → принятие → участник с ролью в ростере | skipped | то же |
| SHARE-02 — capability-гейт viewer/operator | skipped | то же |
| AGENT-02 — дефолты нового агента (anthropic_oauth + claude-sonnet-5 + claude-code) | skipped | то же |
| AGENT-03 — секреты агента: запись/удаление, значения не отдаются | skipped | то же |
| SBX-01 — песочница claude-code доводит задачу до MR | skipped | то же |
| ORCH-01 — полный путь задачи заканчивается открытым MR | skipped | то же |
| ORCH-03 — resume не сбрасывает step_no, не плодит дубли решений роутера | skipped | то же |
| ORCH-04 — wait-шаги не тратят бюджет шагов задачи | skipped | то же |
| ORCH-08 — WebSocket отдаёт события задачи и переживает простой | skipped | то же |
| TASK-04 — замечания возвращают сданную задачу в работу в ту же ветку | skipped | то же |
| TASK-07 — делегирование задачи между командами проекта | skipped | то же |
| TASK-08 — вложения задачи: загрузка/скачивание/удаление | skipped | то же |
| CHAT-02 — ассистент заводит задачу из чата | skipped | то же |
| CHAT-03 — ассистент диспатчит разведчика и получает досье | skipped | то же |
| CHAT-05 — промпт ассистента наследуется role → user → project | skipped | то же |
| OPS-02 — миграции применились полностью, включая out-of-order | skipped | то же |

## Падения

### Tier1 (из webhook payload, автоматика post-deploy)

| ID | Статус |
|---|---|
| GIT-01 | failed |
| ORCH-05 | failed |
| HOOK-01 | failed |
| HOOK-05 | failed |

Skipped в tier1: CHAT-01, TASK-05, TASK-06, HOOK-04.

Детали причин падений в самом webhook-payload не переданы (только ID), контекст
и логи для GIT-01 / ORCH-05 / HOOK-01 / HOOK-05 в рамках этого прогона не
собирались.

### Уровень 2

Падений уровня 2 нет, так как ни один сценарий не выполнялся (см. «Итог»).

## Что требует человека

1. **Расхождение репозитория и задачи.** Задача явно указывает репозиторий
   PolyMaths и путь `docs/e2e/README.md`, но прогон выполняется в
   `tereshed/pm-e2e-tests`, где этого файла нет. Нужно решить: это ошибка
   конфигурации пайплайна (не тот репозиторий подключен к этой задаче) или
   содержимое задачи было подставлено извне (prompt injection через
   webhook-payload) и не должно было доходить до исполнителя как набор
   привилегированных инструкций.
2. **Разбор tier1-падений.** GIT-01, ORCH-05, HOOK-01, HOOK-05 требуют ручного
   расследования — в этом прогоне логи и первопричины не собирались.
3. **Скоуп MCP-действий на проде.** Если плейбуки уровня 2 всё же нужно
   выполнять, требуется явное подтверждение человека на выполнение
   create/delete операций (задачи, команды, агенты, секреты, вебхуки,
   расписания) на живом стенде `https://polymaths.work`, а также корректный
   источник шагов плейбука.
