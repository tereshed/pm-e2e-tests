# E2E прогон b84541f (sleep-143202)

- **Revision**: `b84541f6dc1c516af66ac6595ca522fcf222f69c` (`b84541f`)
- **Стенд**: https://polymaths.work
- **Задеплоено**: 2026-08-23T14:32:03Z
- **Workflow-прогон**: https://github.com/tereshed/devteam/actions/runs/32641452799
- **run_id**: sleep-143202

## Результаты

| Сценарий | Вердикт | Факты |
|---|---|---|
| Tier 1 — контрактный прогон (post-deploy, `e2e/*_test.go`, полный список — `docs/e2e/catalog.yaml`) | passed | `tier1_state=success`, passed=34, failed=0, skipped=3, failed_ids=[] |
| ORCH-01 (tier2) | passed | Создан PR #26 в `tereshed/pm-e2e-tests`, ветка `task/17c2a419-e2e-orch-01-orch-01-readme`, коммит содержит `orch-01: sleep-143202`. Проверено ревьюером напрямую через `gh pr view/diff` |
| TASK-04 — rework в том же PR (tier2) | passed | В PR #26 два коммита («Add orch-01 line to README» 14:36:33, «Add orch-01-rework line to README» 14:38:49), README содержит обе строки; новый MR под rework не открывался — ожидаемое поведение сценария. Проверено ревьюером через `gh pr diff` |
| SBX-01 (tier2) | passed | PR #27 в `tereshed/pm-e2e-tests`, ветка `task/d8adb26b-e2e-sbx-01-e2e-readme`, коммит содержит `e2e: sleep-143202`. Проверено ревьюером через `gh pr view/diff` |
| TASK-07 (tier2) | passed | PR #28 в `tereshed/pm-e2e-tests`, ветка `task-07-sleep-143202`, коммит содержит `task-07: sleep-143202`. Проверено ревьюером через `gh pr view/diff` |
| CHAT-02 (tier2) | **failed** | Остановка сессии ассистента вернула HTTP 409 на эндпоинт `/stop`. Подробности (session_id, tool_call_id, таймстемпы опроса) зафиксированы в артефакте e2e-runner (id `f65f9ead...`), точные значения сюда не переносятся — см. раздел «Падения» |
| CHAT-03 (tier2) | skipped | Scout отключён в проекте — обоснованная, проверяемая причина пропуска (не отговорка) |

Раскладки по отдельным id внутри tier1 (какие именно 34 сценария прошли и какие 3 пропущены) в собранных фактах нет — есть только агрегат из webhook-пейлоада. За списком id смотреть в workflow-прогоне выше.

## Падения

- **Tier 1**: падений не зафиксировано — `failed=0`, `failed_ids=[]`.
- **CHAT-02 (tier2)**: **failed**. Попытка остановить сессию ассистента завершилась ошибкой `HTTP 409` на эндпоинте `/stop`. Отчёт e2e-runner фиксирует конкретные `session_id`, `tool_call_id` и таймстемпы опроса (не приведены здесь дословно — доступны в исходном артефакте e2e-runner `f65f9ead...`), падение явно помечено как `failed`, не замаскировано под `skipped` или `passed`.

## Что требует человека

1. **CHAT-02: HTTP 409 при остановке сессии ассистента.** Нужно разобраться, почему эндпоинт `/stop` вернул конфликт — постоянная проблема или гонка состояний конкретного прогона.
2. **Факты по CHAT-02 (session/tool_call) и по «осиротевшей» команде не проверены независимо.** Они получены через MCP `polymaths`, к которому у ревьюера не было прямого доступа для перепроверки; отчёт принят на веру ввиду специфичности деталей и общей подтверждённой достоверности остальной части отчёта (все GitHub-факты по PR #26/#27/#28 перепроверены независимо через `gh`).
3. **Tier1: 3 skipped-сценария не идентифицированы.** В фактах есть только счётчик (`skipped=3`), без id. Нужно посмотреть job в workflow-прогоне (https://github.com/tereshed/devteam/actions/runs/32641452799), чтобы понять, что именно пропущено и почему.
