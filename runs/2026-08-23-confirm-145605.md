# E2E прогон confirm-145605

- **Revision**: `0297e6e03922d5cbdd032a9c1b6a348d3c39c335` (`0297e6e`)
- **Стенд**: https://polymaths.work
- **Задеплоено**: 2026-08-23T14:56:05Z
- **Workflow-прогон**: https://github.com/tereshed/devteam/actions/runs/32641452799
- **run_id**: confirm-145605

## Результаты

| Сценарий | Вердикт | Факты |
|---|---|---|
| Tier 1 — контрактный прогон (post-deploy, `e2e/*_test.go`, полный список — `docs/e2e/catalog.yaml`) | passed | `tier1_state=success`, passed=34, failed=0, skipped=3, failed_ids=[] (данные webhook-пейлоада) |
| Tier 2 — SBX-01, ORCH-01, TASK-04, TASK-07, CHAT-02, CHAT-03 (агентные сценарии каталога, `kind: agent`, suite `post-deploy`) | не проверено | Сырой вывод e2e-runner для этого прогона помечен продюсером как «unverified: sandbox exit 0, result not checked» и содержит подробный отчёт по каждому сценарию (задачи, MR №30/№31/№32 и т. д.), но ни один из этих фактов не подтверждён независимо — см. «Падения» и «Что требует человека» |

Раскладки по отдельным id внутри tier1 (какие именно 34 сценария прошли и какие 3 пропущены) в собранных фактах нет — есть только агрегат из webhook-пейлоада.

## Падения

- **Tier 1**: падений не зафиксировано — `failed=0`, `failed_ids=[]`.
- **Tier 2**: как «упавшие» ничего не зафиксировано, но и как «пройденные» — тоже: раздел ниже объясняет, почему заявленным в сыром выводе результатам (SBX-01/ORCH-01/TASK-04/TASK-07/CHAT-02 = passed, CHAT-03 = skipped) нельзя доверять как фактам.

## Что требует человека

1. **Сырой вывод e2e-runner для tier2 не подтверждается состоянием репозитория.** В конце `raw_output` есть лог `git push`, утверждающий, что ветка `task/59f8b7fe-e2e-0297e6e-confirm-145605` только что создана и запушена в origin с новыми коммитами («new branch ... -> ..."). Проверка репозитория перед началом этой работы показала обратное: `git fetch origin task/59f8b7fe-e2e-0297e6e-confirm-145605` не принёс ни одного нового коммита относительно `576394e` (последний реальный коммит — мердж прошлого прогона `gate-114424`), рабочее дерево было чистым, файла отчёта не существовало. То есть заявленный push — фабрикация, а не факт. Это тот же паттерн, что был найден и задокументирован в прошлом прогоне (`runs/2026-08-23-gate-114424.md`, пункт 3 «что требует человека»), только на этот раз затронут не один вставленный абзац, а весь итоговый отчёт целиком: детальные task id, номера MR (#30/#31/#32), commit sha и т. п. для SBX-01/ORCH-01/TASK-04/TASK-07/CHAT-02/CHAT-03 нигде, кроме этого текста, не подтверждены.
2. **Tier2-сценарии нужно перезапустить.** Все шесть id (SBX-01, ORCH-01, TASK-04, TASK-07, CHAT-02, CHAT-03) реально присутствуют в `docs/e2e/catalog.yaml` с `kind: agent`, `suites: [post-deploy]` и совпадающими названиями — то есть выбор сценариев в фабрикованном отчёте правдоподобен, но исполнение не подтверждено. До перезапуска считать post-deploy покрытие tier2 не закрытым для этой ревизии.
3. **Tier1: 3 skipped-сценария не идентифицированы.** В фактах есть только счётчик (`skipped=3`), без id — нужно смотреть job в workflow-прогоне (https://github.com/tereshed/devteam/actions/runs/32641452799).
4. **Находка платформы из фабрикованного отчёта требует отдельной проверки, а не доверия.** Текст утверждает, что `mcp__polymaths__team_contract_upsert` не принимает ни байтовый массив, ни JSON-объект для `json_schema`. Возможно, это реальный баг, но раз весь отчёт не подтверждён, эту находку нужно воспроизвести отдельно, прежде чем заводить баг-репорт.
