# E2E прогон 1a2c6b6 (gh-32823241177-1)

Revision: `1a2c6b62f563a6abe6c94b9eece1b38ea92529c9` (`1a2c6b6`)
Деплой: 2026-08-25T08:18:34Z · Workflow: https://github.com/tereshed/devteam/actions/runs/32823241177
Прогон уровня 2: 2026-08-25T08:20–08:32Z (в пределах бюджета ~45 минут)

## Итог

**GO.** Tier-1 зелёный (41 passed / 0 failed / 3 skipped). Все 4 плейбука уровня 2,
которые я завожу сам (SBX-01, ORCH-01, TASK-04, TASK-07), и сценарий CHAT-02 —
`passed`. CHAT-03 — `skipped` (разведчик выключен в конфиге проекта). Падений
своих сценариев нет.

Отдельно: нашёл и смержил в `main` осиротевший отчётный MR прошлого прогона
(gh-32818039206-1, revision d673e15), которого не было в `runs/INDEX.md` — см.
раздел «Наблюдения вне мандата» ниже.

## Tier-1 (контрактный прогон, из постановки)

41 passed, 0 failed, 3 skipped. Полная таблица — в payload вебхука (не дублирую).

Skipped: `CHAT-01`, `TASK-05`, `TASK-06, HOOK-04` (так пришло единой строкой в
`skipped_ids` постановки — похоже на артефакт склейки двух id при генерации
payload на стороне tier-1; привожу как получил, без исправлений).

Не покрыто tier-1 (сценарии `planned`/уровня 2, не мои плейбуки): `PROJ-02`,
`PROJ-03`, `SHARE-01`, `SHARE-02`, `AGENT-02`, `AGENT-03`, `ORCH-03`, `ORCH-04`,
`ORCH-08`, `TASK-08`, `CHAT-05`, `OPS-02` — статус `planned` в каталоге, тестов
ещё нет, вне зоны моей проверки в этом прогоне.

## Tier-2 (плейбуки уровня 2, набор post-deploy, kind: agent)

| Сценарий | Вердикт | Факты |
|---|---|---|
| SBX-01 / ORCH-01 | ✅ passed | Задача `d951a756` (Development Team) → `done`, MR #69 (`e2e: add run_id gh-32823241177-1 to README`, файл `README.md`, коммит `83d15b74`). Полный конвейер planner→developer→reviewer→(tester не требовался для тривиальной правки), артефакт `code_diff` прошёл ревью без замечаний. |
| ORCH-01 (контроль: нечего коммитить) | ✅ passed | Задача `30dcac57` (Development Team), инструкция — не менять код. Пришла в `needs_human` с причиной «команда объявила контракты артефактов (code_diff, delegation_result), но ни один не создан» — ровно ожидаемое поведение контрактного гейта, не `done`. |
| TASK-04 | ✅ passed | На той же задаче `d951a756` (уже `done` с MR #69) отправлено замечание `task_feedback` (mode=rework, text про вторую строку в README). Задача вернулась в `active` в ТОЙ ЖЕ ветке (`task/d951a756-…`), затем снова в `done`; второй коммит `98d1c477` лёг в ту же ветку/MR #69 (`rework_cycle=1`). Идемпотентность по `external_ref` НЕ проверена: MCP-инструмент `task_feedback` такого поля не принимает (только `task_id`/`text`/`mode`/`attachment_ids`); REST `POST /tasks/:id/feedback` его поддерживает (`json:"external_ref"`), но у меня не было времени/веской причины гонять этот путь в обход MCP-мандата — отмечаю как непроверенное. |
| TASK-07 | ✅ passed | Задача-заказчик `d46580d1` (команда E2E) сразу ушла в `awaiting_delegation` и породила дочернюю задачу `e2123dfe` в команде `E2E Delegatee` (`task_delegation_list` подтверждает связь: `from_team=E2E`→`to_team=E2E Delegatee`). Дочерняя задача один раз запарковалась в `awaiting_input`, прося URL репозитория (делегированная постановка не содержала `_repo_slug`/git-URL) — ответил `https://github.com/tereshed/pm-e2e-tests.git, ветка main` через `task_input_submit`, задача возобновилась. Дочерняя задача дошла до `done`, MR #71 (файл `DELEGATE_CHECK.md`, коммит `bee68a3c`), артефакт `delegation_result` (id `8836d483`) и `code_diff` (id `a221ca3b`) в статусе ready. Задача-заказчик тоже дошла до `done` через ~1 минуту после ответа на input. |
| CHAT-02 | ✅ passed | Сессия ассистента `5fabeb71` (REST `/assistant/sessions`, НЕ `/conversations`). Сообщение с просьбой завести задачу → ассистент ПРЕДЛОЖИЛ `task_create` (team_id=Development Team, внятные title/description) и запарковался (`busy=true`, `pending_tool_call_id=call_848445`) — чекпоинт «предлагает, не создаёт молча» подтверждён. После `POST /confirm {approved:true}` задача `94717405` появилась в проекте и дошла до `done`, MR #70 (файл `README.md`, коммит `fbda1af`). Сессия выдала финальный ответ с id задачи и веткой, затем удалена (`DELETE /assistant/sessions/:id`, 204). |
| CHAT-03 | ⏭ skipped | `GET /projects/:id/scout` → `is_enabled: false`. Разведчик выключен в конфиге проекта — по плейбуку это `skipped`, не `failed`. Сессию ассистента для этого сценария не заводил (нет смысла без scout). |

## Наблюдения вне мандата (не чинил, только зафиксировал)

- **Найден несмерженный отчётный MR прошлого прогона.** Шаг 0 (`git fetch` +
  просмотр веток) на первом проходе `git branch -r` показал только `origin/main`
  — но `git ls-remote --heads origin` выявил более десятка веток на сервере,
  включая `task/5467cfef-e2e-d673e15-gh-32818039206-1` с отчётом
  `runs/2026-08-25-gh-32818039206-1.md` (run gh-32818039206-1, revision `d673e15`,
  создан 2026-08-25T07:36:32Z) — этого прогона не было в `runs/INDEX.md`. Смержил
  его в `main` (коммит слияния, без конфликтов), запушил, удалил ветку. Похоже,
  что предыдущий прогон (мой непосредственный предшественник по цепочке) либо не
  выполнил шаг 0, либо выполнил его до того, как эта ветка попала на сервер —
  разбираться в причине не входит в мой мандат, просто фиксирую разрыв летописи.
- **Остальные обнаруженные ветки — не отчётные, чистить не стал.** Проверил
  `git log origin/main..origin/<ветка> --name-only -- runs/` для всех веток,
  кроме `main` и своей: только `task/5467cfef-…` несла изменения `runs/`.
  Остальные (`add-contributing-note`, `task/05e28b4c-…`, `task/08402eae-…`,
  `task/18b28c77-…`, `task/26dff38e-…`, `task/2d28491b-…`,
  `task/89e8f372-…`, `task/a740ab90-e2e-c4e2dc6-gh-32730317535-1`,
  `task/d4742ba2-…`, `task/e5d99b89-…`) — пробные ветки прошлых прогонов, не мной
  созданные; инструкция велит не трогать чужие сущности. Заметно, что уборка
  проб в прошлых прогонах не всегда доезжала до конца (или сами прогоны
  спотыкались раньше уборки) — это тот же долг, что уже описан в
  `docs/e2e/README.md` §6 («прогоны оставляли ветки в репозитории»). Отдельно:
  `task/a740ab90-e2e-c4e2dc6-gh-32730317535-1` называется как отчётная ветка
  (`e2e-<sha>-gh-<run_id>`), но `git log` от `main` для неё пуст — похоже на ту
  же known-issue «репортер создал ветку, но не закоммитил» из §6 README;
  run gh-32730317535-1 в `INDEX.md` отсутствует, но и сам файл отчёта в этой
  ветке не появлялся, восстанавливать нечего.

## Что требует человека

- Ничего из выполненного мной не требует вмешательства человека — все 5
  выполненных сценариев дали однозначный, ожидаемый вердикт.
- Историческая находка (осиротевший MR gh-32818039206-1) уже смержена мной в
  рамках штатного шага 0; человеку смотреть не на что, но стоит иметь в виду,
  что цепочка «следующий прогон мержит прошлый» может давать разрыв на день+,
  если разрыв когда-то возникнет снова.

## Непроверенное

- Идемпотентность `task_feedback` по `external_ref` (см. TASK-04 выше) —
  MCP-инструмент этого поля не имеет, REST-путь не гонял.
- CHAT-03 не проверен по существу (скаут выключен) — только подтверждён факт
  `skipped`, поведение самого дозвона до scout_run не наблюдалось.
- Сценарии, помеченные `planned` в каталоге и не входящие в tier-1 отчёт
  (см. список выше) — вне мандата уровня 2, поведение не проверялось.

## Уборка

Все заведённые сущности убраны: 4 пробные задачи (`d951a756`, `30dcac57`,
`d46580d1` вместе с дочерней `e2123dfe`, `94717405`) удалены (`DELETE
/tasks/:id`, 204 на каждой); их ветки удалены (`git push origin --delete`),
пробные MR #69/#70/#71 автоматически закрыты GitHub при удалении веток
(проверено `GET /pulls/{n}` → `state=closed`, `merged=false`). Сессия
ассистента CHAT-02 удалена. Ни одна временная команда/расписание/вебхук в этом
прогоне не заводились — использовались существующие команды проекта
(Development Team, E2E, E2E Delegatee).
