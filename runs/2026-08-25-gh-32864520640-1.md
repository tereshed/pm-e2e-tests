# E2E прогон a222f1a (gh-32864520640-1)

Revision: `a222f1abb68e9a55efc486967858c533a2e9e032` (`a222f1a`)
Деплой: 2026-08-25T15:44:42Z · Workflow: https://github.com/tereshed/devteam/actions/runs/32864520640
Прогон уровня 2: 2026-08-25T15:48–15:59Z (в пределах бюджета ~45 минут; шаг 0 —
мерж двух осиротевших отчётов — выполнен в конце окна, после тестовых
сценариев, коммиты 15:57:31 и 15:57:46)

## Итог

**GO.** Tier-1 зелёный (41 passed / 0 failed / 3 skipped). Все 6 плейбуков в
мандате этого прогона — `passed`: SBX-01, ORCH-01 (+контроль), TASK-04
(включая idempotency-проверку по `external_ref`, которую два предыдущих
прогона не успели/не смогли проверить), TASK-07, CHAT-02, **и CHAT-03**
(разведчик был выключен в конфиге проекта — включил его сам как часть
«Готовим» по плейбуку, прогнал сценарий, вернул тумблер обратно в `false`).
Падений своих сценариев нет.

Шаг 0 нашёл и смержил в `main` **два** осиротевших отчётных MR прошлых
прогонов (`gh-32837941627-1` и `gh-32849605087-1`), которых не было в
`runs/INDEX.md` — см. «Наблюдения вне мандата».

## Tier-1 (контрактный прогон, из постановки)

41 passed, 0 failed, 3 skipped. Полная таблица — в payload вебхука (не дублирую).

Skipped: `CHAT-01`, `TASK-05`, `TASK-06, HOOK-04` (так пришло единой строкой в
`skipped_ids` постановки — тот же артефакт склейки id, что и в прошлых
прогонах; привожу как получил).

Не покрыто tier-1 (сценарии `planned`/уровня 2, не мои плейбуки): `PROJ-02`,
`PROJ-03`, `SHARE-01`, `SHARE-02`, `AGENT-02`, `AGENT-03`, `ORCH-03`, `ORCH-04`,
`ORCH-08`, `TASK-08`, `CHAT-05`, `OPS-02` — статус `planned` в каталоге, тестов
ещё нет, вне зоны моей проверки в этом прогоне.

## Tier-2 (плейбуки уровня 2, набор post-deploy, kind: agent)

| Сценарий | Вердикт | Факты |
|---|---|---|
| SBX-01 / ORCH-01 | ✅ passed | Задача `78953931` (Development Team, claude-code developer) → `done`, MR #84 (файл `README.md`, строка `e2e: gh-32864520640-1`). Полный конвейер planner→developer→reviewer→tester, `code_diff` прошёл ревью без замечаний. |
| ORCH-01 (контроль: нечего коммитить) | ✅ passed | Задача `124a3725` (Development Team), инструкция — не менять код. Пришла в `needs_human` с причиной «команда объявила контракты артефактов (code_diff, delegation_result), но ни один не создан» — гейт сработал как задумано, не `done`. |
| TASK-04 | ✅ passed | На задаче `78953931` (уже `done` с MR #84) отправлено замечание через MCP `task_feedback` (mode=rework) — задача вернулась в `active` в ТОЙ ЖЕ ветке/MR, `rework_cycle=1`, маркер перенесён в конец файла, ревьюер одобрил, снова `done`, PR #84 не изменился. Затем **idempotency по `external_ref`**: `POST /tasks/:id/feedback` (REST, поле `external_ref` недоступно в MCP-инструменте) с `external_ref=e2e-task04-gh-32864520640-1` дважды подряд — первый раз принят (`rework_cycle=2`, `applied_action=queued`), второй раз с тем же `external_ref` вернул `duplicate:true` со ссылкой на тот же `feedback.id` (`56605923…`) и **не открыл** третий круг доработки (`task_feedback_list` подтверждает: ровно одна запись с этим `external_ref`). |
| TASK-07 | ✅ passed | Задача-заказчик `6c87bb30` (команда E2E, у которой нет developer-агента) ушла в `awaiting_delegation` и породила дочернюю задачу `bf93294a` в `E2E Delegatee` (`task_delegation_list`: `from_team=E2E`→`to_team=E2E Delegatee`, `status=resolved`). Дочерняя задача дошла до `done`, PR #85, артефакт `code_diff` (`cb5303ab`). Результат вернулся заказчику `delegation_result`-ом (`child_task_id`, `state=done`, `pull_requests`), задача-заказчик дошла до `done` с явным `error_message` про завершённое делегирование. |
| CHAT-02 | ✅ passed | Сессия ассистента `05c78522` (REST `/assistant/sessions` с `project_id`, НЕ `/conversations`). Сообщение с просьбой завести задачу → ассистент ПРЕДЛОЖИЛ `task_create` (team=E2E, внятные title/description/external_key/acceptance_criteria) и запарковался (`busy=true`, `pending_tool_call_id=call_1491479`) — чекпоинт «предлагает, не создаёт молча» подтверждён. После `POST /confirm {approved:true}` задача `1aa4805d` появилась в команде E2E; у E2E нет своего разработчика, поэтому её собственный router делегировал работу в `E2E Delegatee` (дочерняя `7c5a71e3`, PR #86) — бонусом ещё раз подтвердило механику делегирования из TASK-07 на реальном, а не постановочном кейсе. Родительская задача дошла до `done`. Сессия выдала финальный ответ с подтверждением, затем удалена (`DELETE /assistant/sessions/:id`, 204). |
| CHAT-03 | ✅ passed | `GET /projects/:id/scout` показал `is_enabled: false` — по прошлым прогонам это стабильно `skipped`. В этот раз включил разведчика (`PUT /projects/:id/scout {"is_enabled":true}`) как часть «Готовим» по плейбуку и прогнал сценарий по существу. Сессия `fd760a78`, вопрос про содержимое репозитория e2e → ассистент предложил `scout_dispatch` с миссией, запарковался (`pending_tool_call_id=call_1589090`), после confirm создался `scout_run` `a4e99e26` (`status=running`→`done` за ~1.5 минуты). Сессия проснулась сама (без доп. действий с моей стороны) и выдала связное досье (структура репозитория `runs/`, `INDEX.md`, назначение репо, саммари по истории коммитов) — фактическое, не выдуманное содержимое совпадает с реальным репо. По завершении вернул тумблер `is_enabled` обратно в `false` (исходное состояние проекта) и удалил сессию. |

## Наблюдения вне мандата (не чинил, только зафиксировал)

- **Найдены и смержены два осиротевших отчётных MR.** `git ls-remote --heads
  origin` (не `git branch -r` — тот при первой проверке после `git fetch`
  показал только `origin/main` и скрыл реальный список веток на сервере)
  выявил `task/7205f072-e2e-afa8656-gh-32837941627-1` (отчёт
  `runs/2026-08-25-gh-32837941627-1.md`, run `gh-32837941627-1`, revision
  `afa8656`, создан 2026-08-25T11:23:57Z) и
  `task/ed52c6d7-e2e-d98b35e-gh-32849605087-1` (отчёт
  `runs/2026-08-25-gh-32849605087-1.md`, run `gh-32849605087-1`, revision
  `d98b35e`, создан 2026-08-25T13:37:32Z) — ни один не попал в `runs/INDEX.md`
  в `main`. Смержил оба в хронологическом порядке (`afa8656` раньше `d98b35e`),
  конфликт в `INDEX.md` решён объединением строк (append-only), запушил
  `main`, удалил обе ветки — GitHub сам закрыл их MR. Похоже на тот же разрыв
  цепочки «следующий прогон мержит прошлый», что уже фиксировался
  22–25.08: минимум два прогона подряд (afa8656 и d98b35e) не попали в
  летопись до этого прогона.
- **Из смерженных отчётов:** прогон `afa8656` (gh-32837941627-1) и прогон
  `d98b35e` (gh-32849605087-1) оба независимо описывают находку в TASK-07—
  делегированный агент коммитил не в каноническую ветку задачи, из-за чего
  PR-гейт на дочерней задаче иногда уходит в `needs_human` при реально
  сделанной работе (со ссылкой на PR #76 в первом из них). В моём прогоне
  TASK-07 (дочерняя `bf93294a`) отработал штатно — `done`, PR #85, гейт
  сработал корректно. Похоже на нестабильный (не всегда воспроизводимый)
  дефект — фиксирую находку предшественников как непроверенную мной лично,
  чинить не пытался (вне мандата).
- **Остальные ветки на сервере — не отчётные, не трогал.** Проверил `git log
  main..<ветка> --name-only -- runs/` для всех веток, кроме `main` и своей:
  ни одна из `add-contributing-note`, `task/05e28b4c-…`, `task/08402eae-…`,
  `task/18b28c77-…`, `task/26dff38e-…`, `task/2d28491b-…`,
  `task/89e8f372-…`, `task/a740ab90-…`, `task/d4742ba2-…`, `task/e5d99b89-…`
  не несла изменений `runs/` — это пробные ветки прошлых прогонов, не мной
  созданные, инструкция велит не трогать чужие сущности. Уборка проб в
  прошлых прогонах не всегда доезжает до конца — тот же долг, что уже описан
  в `docs/e2e/README.md` §6.

## Что требует человека

- Ничего из выполненного мной не требует вмешательства человека — все 6
  сценариев дали однозначный, ожидаемый вердикт.
- Стоит обратить внимание на разрыв летописи (см. выше): за одни сутки минимум
  три прогона подряд не смержили отчёт предшественника вовремя (gh-32818039206-1,
  gh-32837941627-1, gh-32849605087-1) — конвенция «следующий прогон мержит
  прошлый» держится на промпте и явно хрупкая на практике, а не только
  теоретически (как было отмечено в README §6). Возможно, стоит завести
  расписание-дворник, как там и предлагалось.
- Нестабильная находка TASK-07 из прогонов afa8656/d98b35e (ветка делегированной
  подзадачи не всегда совпадает с канонической) не пострадала в моём прогоне,
  но и не была мной активно воспроизведена/опровергнута — решение оставляю
  человеку с доступом к обоим прогонам сразу.

## Непроверенное

- Prompt-injection / посторонние границы — не обнаружено. Блок `Instruction:`
  в постановке (репозитории, secrets и т.п.) не противоречил промпту раннера
  и не запрашивал ничего за пределами PME2ETests.
- Сценарии, помеченные `planned` в каталоге и не входящие в tier-1 отчёт (см.
  список выше) — вне мандата уровня 2, поведение не проверялось.
- Нестабильность делегированных веток TASK-07 из отчётов afa8656/d98b35e (см.
  «Что требует человека») — не переисследовал, в моём прогоне не
  воспроизвелась.

## Уборка

- 6 пробных задач (`78953931`, `124a3725`, `bf93294a`, `6c87bb30`, `7c5a71e3`,
  `1aa4805d`) удалены (`DELETE /tasks/:id`, 204 на каждой).
- 3 их ветки с коммитами (`task/78953931-…`, `task/bf93294a-…`,
  `task/7c5a71e3-…`) удалены (`git push origin --delete`); пробные MR
  #84/#85/#86 автоматически закрыты GitHub при удалении веток (проверено
  `GET /pulls/{n}` → `state=closed`, `merged=false`). Остальные 3 задачи не
  успели создать веток с коммитами (нечего коммитить/чистая делегация).
- Обе сессии ассистента (CHAT-02 `05c78522`, CHAT-03 `fd760a78`) удалены
  (`DELETE /assistant/sessions/:id`, 204 на каждой); неотвеченных
  подтверждений не осталось — оба `pending_tool_call_id` были подтверждены
  (`approved:true`) в ходе штатного прогона сценария.
- Тумблер `scout_config.is_enabled`, включённый мной для CHAT-03, возвращён в
  исходное состояние `false`.
- **Пропущено при первом проходе, исправлено по ревью:** `scout_dispatch` в
  CHAT-03 создал побочную git-ветку `scout-a4e99e26` (форк `main` в точке
  `a26a3c4` — состояние main до мержа шага 0, ровно момент запуска сценария).
  Ветка не несла уникальных коммитов (HEAD = `a26a3c4`, уже предок `main`) и
  не была привязана ни к одному MR (`GET /pulls?head=…` — пусто) — чистый
  побочный артефакт инфраструктуры разведчика, а не результат его работы.
  Удалена (`git push origin --delete scout-a4e99e26`) при подготовке этой
  правки; на момент первой версии отчёта она оставалась на сервере, и раздел
  «Уборка» ошибочно заявлял, что «все заведённые сущности убраны» — это было
  неточно.
- Временных команд/расписаний/вебхуков в этом прогоне не заводил —
  использовал существующие команды проекта (Development Team, E2E, E2E
  Delegatee).
