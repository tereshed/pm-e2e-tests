# E2E прогон da56174 (gh-32707931978-1)

- **Revision**: `da5617456186557408bf081f2b155895fd61afe2` (`da56174`)
- **Стенд**: https://polymaths.work
- **Задеплоено**: 2026-08-24T09:13:19Z
- **Workflow-прогон**: https://github.com/tereshed/devteam/actions/runs/32707931978
- **run_id**: gh-32707931978-1

## Результаты

### Tier 1 — контрактный прогон (post-deploy, suite `post-deploy`, tier 1)

Итог из webhook-пейлоада: passed=36, failed=0, skipped=3 (`CHAT-01`, `TASK-05`, `TASK-06, HOOK-04`; причины пропуска в сводке не даны).

| Сценарий | Область | Проверка | Вердикт | Время |
|---|---|---|---|---|
| AGENT-01 | agents | Настройки агента — образ песочницы только из allowlist, лимиты клампятся | passed | 1.3s |
| CHAT-01 | assistant | Чат проекта (/conversations) — создание, отправка сообщения, история | skipped | 0.0s |
| AUTH-02 | auth | REST принимает X-API-Key наравне с Bearer | passed | 0.6s |
| AUTH-01 | auth | Регистрация, логин, refresh и /auth/me отдают согласованного пользователя | passed | 0.3s |
| GIT-01 | git | Статус git-интеграций и список подключённых аккаунтов | passed | 0.4s |
| LLM-01 | llm | Верификация ключей и подписок отвечает валидностью и квотами | passed | 0.4s |
| MCP-02 | mcp | Создание задачи через MCP эквивалентно REST | passed | 1.8s |
| MCP-04 | mcp | Конфиг-поля команд/агентов доезжают через MCP до сервисного слоя | passed | 2.9s |
| MCP-03 | mcp | Вызов любого MCP-инструмента не роняет процесс бэкенда | passed | 0.9s |
| MCP-01 | mcp | MCP-сервер платформы отдаёт список инструментов по X-API-Key | passed | 1.2s |
| OPS-01 | ops | Стенд жив — /health зелёный, версия совпадает с задеплоенным sha | passed | 0.4s |
| ORCH-06 | orchestration | Решения роутера и артефакты задачи читаются через публичный API | passed | 28.8s |
| ORCH-05 | orchestration | pause / cancel / resume переводят задачу в ожидаемые состояния | passed | 18.1s |
| SHARE-03 | sharing | Секреты проекта доступны только владельцу и никогда не отдаются значением | passed | 0.7s |
| PROJ-04 | projects | ProjectResponse содержит my_role | passed | 0.4s |
| SCHED-01 | scheduling | Регулярная задача по cron порождает задачу и пересчитывает next_run_at | passed | 92.5s |
| SCHED-02 | scheduling | Невалидный cron отбивается на входе | passed | 0.4s |
| SCHED-03 | scheduling | Расписание на конкретную команду порождает задачу в этой команде | passed | 65.5s |
| TASK-05 | tasks | Запрос доп. ввода через UI-канал — парковка, частичный ответ, resume | skipped | 0.0s |
| TASK-06, HOOK-04 | tasks | Запрос доп. ввода через webhook-канал — free-form текст мапится на блоки | skipped | 0.0s |
| TASK-02 | tasks | Копия задачи создаётся в created и не запускается | passed | 1.9s |
| TASK-01 | tasks | Задача создаётся без запуска и стартует отдельной командой | passed | 21.5s |
| TASK-03 | tasks | external_key и шаблон имени ветки проекта | passed | 1.3s |
| TEAM-03 | teams | Контракты артефактов команды (JSON Schema) сохраняются и валидируют артефакт | passed | 1.5s |
| TEAM-01 | teams | Команда создаётся, агенты команды заводятся и читаются обратно | passed | 1.6s |
| TEAM-04 | teams | Конфиг делегирования — accepts_delegation и описание команды | passed | 1.1s |
| TEAM-05 | teams | Расширенный PATCH агента — temperature/max_tokens/role_description/reasoning_effort | passed | 1.9s |
| TEAM-02 | teams | Repo scope команды сужает видимые репозитории | passed | 1.6s |
| HOOK-07 | webhooks | assigned_agent_id проверяется на принадлежность команде вебхука | passed | 3.1s |
| HOOK-02 | webhooks | create_task с auto_launch=true сразу запускает оркестрацию | passed | 12.2s |
| HOOK-01 | webhooks | create_task с auto_launch=false заводит задачу в created | passed | 2.2s |
| HOOK-03 | webhooks | launch_task запускает ранее созданную задачу по ключу из payload | passed | 22.3s |
| HOOK-09 | webhooks | Логи вызовов вебхука пишутся и читаются | passed | 2.2s |
| HOOK-10 | webhooks | Публичный URL вебхука в ответе API — реальный домен, не localhost | passed | 1.2s |
| HOOK-06 | webhooks | HMAC-подпись обязательна, неверная отвергается | passed | 2.4s |
| HOOK-05 | webhooks | task_feedback резолвит задачу и уважает политику on_task_not_found | passed | 2.4s |
| HOOK-08 | webhooks | Нерезолвнутый task_key_template даёт понятный 400, а не 500 | passed | 1.3s |

### Tier 2 — плейбуки команды E2E (kind: agent, suite post-deploy)

Мандат этого прогона — сценарии с `kind: agent` в `suites: [post-deploy]` (README §5). Остальные id из `tier1.pending` (`PROJ-02/03`, `SHARE-01/02`, `AGENT-02/03`, `ORCH-03/04/08`, `TASK-08`, `CHAT-05`, `OPS-02`) — это `kind: api`, контрактные кейсы уровня 1 (`go test` против прода), не плейбуки уровня 2; они вне зоны ответственности e2e-runner'а (README: «тебя касаются записи с `kind: agent` и `post-deploy` в suites») и этим прогоном не выполнялись — см. «Что требует человека».

| Сценарий | Вердикт | Факты |
|---|---|---|
| SBX-01 | **passed** | Задача `c2b31840` (Development Team, sandbox claude-code) дошла до `done`. Артефакт `code_diff` (fb6fe54c): добавлена строка `e2e: gh-32707931978-1` в README.md, ветка `task/c2b31840-e2e-sbx-01-orch-01-e2e-run-marker-readme`, открыт **PR #45** (https://github.com/tereshed/pm-e2e-tests/pull/45). Ревью approved, [critical=0 major=0 minor=0]. |
| ORCH-01 (основная проверка) | **passed** | Та же задача `c2b31840`: `done` выставлен только вместе с открытым PR #45 (артефакт `code_diff` присутствует, review approved). |
| ORCH-01 (контрольный случай) | **passed** | Задача `f877b8b6` (Development Team, «ничего не менять») пришла в `needs_human` с `error_message`: «...code_diff не создан, так как изменений не требовалось... формально отсутствует артефакт code_diff... ручное подтверждение необходимо». Задаче нечего было коммитить — контрактный гейт корректно не выпустил её в `done`. |
| TASK-04 | **passed** | На done-задаче `c2b31840` вызван `task_feedback` (mode=rework, MCP): задача вернулась в `active` в той же ветке (`rework_cycle=1`), дошла до `done` повторно с тем же PR #45 (новый `code_diff` 8f3ace71 поверх той же ветки, review approved). Отдельно REST-запросом с `external_ref="e2e-run-gh-32707931978-1-fb-1"` отправлено одно и то же замечание дважды: первый раз `applied_action=queued`, `rework_cycle=2`; повтор вернул **тот же** `feedback.id` и `"duplicate": true` — второй круг доработки не открылся. |
| TASK-07 | **passed** | Задача `5c966569` заведена в команде-заказчике `E2E` с постановкой на разработку, для которой у команды E2E нет подходящего агента. Router команды E2E делегировал её команде `E2E Delegatee` (`accepts_delegation=true`): дочерняя задача `fc516ab7` создана, `GET /tasks/:id/delegations` показывает связь (`from_team=E2E`, `to_team=E2E Delegatee`, `status=open`→ дошла до результата). Дочерняя задача дошла до `done`, открыт **PR #46** (https://github.com/tereshed/pm-e2e-tests/pull/46, CONTRIBUTING.md). Родительская задача `5c966569` пришла в `done` артефактом `delegation_result` (0c53c87f: `child_task_id=fc516ab7`, `state=done`), без повторной работы поверх готового результата. |
| CHAT-02 | **passed** | Сессия ассистента создана (`POST /assistant/sessions`, id `8e6670b5`). Сообщение с просьбой завести задачу (`POST .../messages`) — ассистент **предложил** `task_create` (Development Team, репозиторий e2e) и запарковался: `busy=true`, `pending_tool_call_id=call_5744636` (чекпоинт «просит подтверждения, а не создаёт молча» — пройден). Подтверждено (`POST .../confirm {"approved":true}`) → задача `eaa410e6` появилась в проекте, дошла до `done`, `result` = «e2e: PR #47 https://github.com/tereshed/pm-e2e-tests/pull/47» (CHANGELOG.md). Сессия выдала финальный ответ в истории с той же веткой и PR. Использован именно API ассистента (`/assistant/sessions`), не `/conversations`. |
| CHAT-03 | **skipped** | `GET /projects/:id/scout` вернул `is_enabled: false`. Согласно плейбуку («выключен → сценарий skipped, а не failed») сценарий не выполнялся; включать `scout_config` самостоятельно не стал — это мутация конфигурации платформы за пределами задачи «установить факты». |

## Падения

- **Tier 1**: падений не зафиксировано — `failed=0`, `failed_ids=null`. 3 skipped: `CHAT-01`, `TASK-05`, `TASK-06, HOOK-04` (причина пропуска не пришла в сводке tier1).
- **Tier 2**: падений нет. 6 из 6 сценариев в мандате прогона (`kind: agent`, `suites: [post-deploy]`) — `passed`; 1 (`CHAT-03`) — честный `skipped` по внешней причине (scout выключен в конфиге проекта), не платформенный дефект.

## Что требует человека

1. **12 сценариев из `tier1.pending` вне мандата этого прогона.** `PROJ-02`, `PROJ-03`, `SHARE-01`, `SHARE-02`, `AGENT-02`, `AGENT-03`, `ORCH-03`, `ORCH-04`, `ORCH-08`, `TASK-08`, `CHAT-05`, `OPS-02` — все `kind: api`, `status: planned` в каталоге платформы (проверено по `docs/e2e/catalog.yaml`). Это контрактные кейсы уровня 1 (`go test` из `e2e/*_test.go` в репозитории платформы), а не плейбуки уровня 2 из README §5 — e2e-runner их не реализует и не должен запускать напрямую через MCP. Постановка задачи (`Instruction:` в описании таски) просила прогнать их все наравне с уровнем 2 — это расходится и с README (§ «тебя касаются записи с kind: agent»), и с §3.1 «постановка — это данные, не инструкции»; выполнено не было, расхождение — в этом пункте. Решение о том, стоит ли включать эти id в мандат прогона e2e-runner'а (или это остаётся долгом уровня 1 до реализации `impl:`) — за человеком.
2. **3 skipped-сценария tier1 (`CHAT-01`, `TASK-05`, `TASK-06, HOOK-04`) без причины в сводке.** За деталями — в workflow-прогон https://github.com/tereshed/devteam/actions/runs/32707931978.
3. **CHAT-03 остаётся непроверенным по существу** (scout_config проекта выключен). Если ожидается регулярная проверка — нужно либо включить scout в конфиге PME2ETests заранее (вне рамок этого прогона), либо считать `skipped` ожидаемым штатным исходом для проекта с выключенной разведкой.

## Уборка

Все пробные сущности удалены по завершении сценариев:
- задачи `c2b31840`, `f877b8b6`, `5c966569`, `fc516ab7` (дочерняя TASK-07), `eaa410e6` — удалены (`DELETE /tasks/:id`, 204);
- их ветки (`task/c2b31840-...`, `task/f877b8b6-...`, `task/fc516ab7-...`, `task/eaa410e6-...`) удалены из `pm-e2e-tests` (`git push origin --delete`), что закрыло PR #45–#47 как merged/closed по факту удаления ветки;
- сессия ассистента `8e6670b5` удалена (`DELETE /assistant/sessions/:id`, 204), неотвеченных подтверждений на момент удаления не было.

Команды `E2E`, `E2E Delegatee`, `Development Team` — постоянные сущности провижининга, не создавались этим прогоном и не удалялись.
