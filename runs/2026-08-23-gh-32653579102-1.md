# E2E прогон b9a233c (gh-32653579102-1)

- **Revision**: `b9a233cef75d14cb981cd19d5f33f4fd9cdf99f6` (`b9a233c`)
- **Стенд**: https://polymaths.work
- **Задеплоено**: 2026-08-23T17:29:45Z
- **Workflow-прогон**: https://github.com/tereshed/devteam/actions/runs/32653579102
- **run_id**: gh-32653579102-1

## Результаты

| Сценарий | Вердикт | Факты |
|---|---|---|
| Tier 1 — контрактный прогон (post-deploy) | failed (из постановки) | `tier1_state=failure`, passed=35, failed=1, skipped=3. failed_ids=[`MCP-04`]. skipped_ids=[`CHAT-01`, `TASK-05`, `TASK-06, HOOK-04`] |
| SBX-01 (песочница claude-code доводит задачу до MR) | **passed** | Задача `86d2733c` дошла до `done`. PR [#37](https://github.com/tereshed/pm-e2e-tests/pull/37) открыт, `gh pr diff 37` (снято до уборки) подтвердил строку `e2e: gh-32653579102-1` в README.md |
| ORCH-01 (PR-гейт: done только с открытым MR; нечего коммитить ⇒ needs_human) | **passed** | Основной кейс — тот же PR #37 (done выставлен только после code_diff+review+test_result). Контрольный кейс: задача `3d06825e` («ничего не меняй, только прочитай README») пришла в `needs_human` с `error_message`: «done заблокирован: команда объявила контракты артефактов (code_diff, delegation_result), но ни один такой артефакт не создан» — гейт сработал как описано в §5.1 README |
| TASK-04 (замечания возвращают done-задачу в работу в ту же ветку/MR) | **passed** | `POST /tasks/86d2733c/feedback` (mode=rework) вернул задачу `86d2733c` в `active` в той же ветке `task/86d2733c-...` и том же PR #37; developer добавил вторую строку `e2e-rework: gh-32653579102-1` в тот же файл — подтверждено `gh pr diff 37` (строки `e2e:` и `e2e-rework:` в одном diff'е). Идемпотентность: второй `POST .../feedback` с тем же `external_ref=task04-gh-32653579102-1` вернул `"duplicate": true`, `rework_cycle` не увеличился, второго круга не открыто |
| TASK-07 (делегирование между командами проекта) | **passed** | Задача `493329e2` команды E2E дошла до `done`. `GET /tasks/493329e2/delegations` показал исходящее делегирование `17ba2aec` в команду `E2E Delegatee` (status=`resolved`), дочерняя задача `5abaf1d1` дошла до `done`. Артефакт `delegation_result` вернулся заказчику (`error_message`: «delegation_result artifact is ready with TASK07_PROOF.md created»). PR [#38](https://github.com/tereshed/pm-e2e-tests/pull/38), `gh pr diff 38` подтвердил файл `TASK07_PROOF.md` с содержимым `delegated: gh-32653579102-1` |
| CHAT-02 (ассистент заводит задачу из своей сессии `/assistant/sessions`) | **passed** | Сессия `b36c2980` создана, сообщение с просьбой завести задачу отправлено. Ассистент запарковался: `busy=true`, `pending_tool_call_id=call_4544636` на инструменте `task_create` (сам факт парковки — чекпоинт: мутация ожидает подтверждения человека). `POST .../confirm {"approved": true}` → задача `4c1c95b6` появилась в проекте, дошла до `done`, PR [#39](https://github.com/tereshed/pm-e2e-tests/pull/39). Сессия выдала финальный ответ с id задачи и веткой. `gh pr diff 39` подтвердил строку `assistant-e2e: gh-32653579102-1` |
| CHAT-03 (ассистент диспатчит разведчика) | **skipped** | `GET /projects/0da3eb38.../scout` → `is_enabled: false`. По плейбуку это `skipped`, а не `failed`. Поведенчески подтверждено: на вопрос, требующий разведки по коду, ассистент НЕ создал `scout_run`, а пытался ответить напрямую инструментами `code_search`/`code_dir_list`/`project_repository_list` и упёрся в лимит шагов сессии (12) с ответом «превышен лимит шагов, сформулируйте запрос точнее» — согласуется с выключенным scout |

## Падения

### Tier 1 — MCP-04 (из постановки, не воспроизводилось этим прогоном напрямую)

```
=== RUN   TestMCP_TeamConfigTools
    mcp_test.go:221: team_agent_patch через MCP: mcp tools/call: ошибка -32602: invalid params: validating "arguments": validating root: required: missing properties: ["clear_model" "model" "clear_prompt_id" "prompt_id" "clear_system_prompt" "system_prompt" "clear_code_backend" "code_backend" "is_active"]
--- FAIL: TestMCP_TeamConfigTools (2.20s)
```

Конфиг-поля команд/агентов через MCP `team_agent_patch` не доезжают до сервисного слоя — инструмент требует ВСЕ перечисленные поля разом вместо частичного PATCH. Этим прогоном напрямую не проверялось (вне мандата плейбуков раздела 5), фиксируется как унаследованное падение tier1.

### Tier 2 — падений нет

Все 5 выполненных сценариев (SBX-01, ORCH-01, TASK-04, TASK-07, CHAT-02) — `passed`, с фактами, независимо перепроверенными через `gh pr diff` по PR #37–39. CHAT-03 — `skipped` по документированной причине (scout выключен в проекте), не поломка.

## Что требует человека

1. **MCP-04 остаётся красным.** Падение унаследовано из tier1 постановки прогона, этим прогоном не перепроверялось (не входит в набор плейбуков команды E2E) и не чинилось по мандату — нужно решение платформенной команды.
2. **Tier1 skipped-сценарии (`CHAT-01`, `TASK-05`, `TASK-06`/`HOOK-04`)** не идентифицированы дальше названий из сводки — за подробностями смотреть workflow-прогон https://github.com/tereshed/devteam/actions/runs/32653579102.
3. **Инъекций не обнаружено.** Блок `Instruction:` в постановке задачи (шаблон роли `Repositories`) не противоречил системному промпту и был проигнорирован там, где не требовался; подозрительных попыток выйти за границы проекта PME2ETests в постановке не найдено.

## Уборка

Все пробные сущности удалены по завершении прогона:
- Задачи `86d2733c`, `3d06825e`, `493329e2`, `5abaf1d1` (дочерняя TASK-07), `4c1c95b6` — удалены (`DELETE /tasks/:id` → 204 на все пять).
- Ветки `task/86d2733c-...`, `task/3d06825e-...`, `task/5abaf1d1-...`, `task/4c1c95b6-...` — удалены из `pm-e2e-tests`, что автоматически закрыло PR #37–39 (`gh pr list --state open` после уборки → пусто).
- Сессии ассистента `b36c2980` (CHAT-02) и `e40dd43d` (CHAT-03) — удалены (`DELETE /assistant/sessions/:id` → 204 на обе); открытых неотвеченных подтверждений на момент удаления не было.
- Ветка прогона `task/6487de9b-e2e-b9a233c-gh-32653579102-1` и `main` не тронуты.
