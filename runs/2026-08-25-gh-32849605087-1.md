# E2E прогон d98b35e (gh-32849605087-1)

Revision: `d98b35effa327943d2d60a747effe7464f2200f4` (`d98b35e`)
Деплой: 2026-08-25T13:22:20Z · Workflow: https://github.com/tereshed/devteam/actions/runs/32849605087
Прогон уровня 2: 2026-08-25T13:24–13:36Z (в пределах бюджета ~45 минут)

## Итог

**GO.** Tier-1 зелёный (41 passed / 0 failed / 3 skipped). Все 4 плейбука уровня 2,
которые я завожу сам (SBX-01, ORCH-01, TASK-04, TASK-07), и сценарий CHAT-02 —
`passed`. Дополнительно прогнал контроль ORCH-01 (нечего коммитить → `needs_human`)
и идемпотентность `task_feedback` по `external_ref` через REST — оба подтвердились.
CHAT-03 — `skipped` (разведчик выключен в конфиге проекта). Собственных падений нет.

При этом в TASK-07 обнаружен реальный дефект платформы (см. «Наблюдения вне
мандата»): PR-гейт делегированной подзадачи проверил не ту ветку и увёл
задачу-заказчика в `needs_human`, хотя работа была сделана и MR реально открыт.
Механика делегирования (создание дочерней задачи, `task_delegation_list`,
`delegation_result`) при этом отработала корректно — вердикт `TASK-07` остаётся
`passed` по чек-листу плейбука, а найденный баг зафиксирован отдельно.

## Tier-1 (контрактный прогон, из постановки)

41 passed, 0 failed, 3 skipped. Полная таблица — в payload вебхука (не дублирую).

Skipped: `CHAT-01`, `TASK-05`, `TASK-06, HOOK-04` (так пришло единой строкой в
`skipped_ids` постановки — похоже на артефакт склейки двух id при генерации
payload на стороне tier-1; привожу как получил, без исправлений).

Не покрыто tier-1 (сценарии `planned`/уровня 1, не мои плейбуки — `kind: api`):
`PROJ-02`, `PROJ-03`, `SHARE-01`, `SHARE-02`, `AGENT-02`, `AGENT-03`, `ORCH-03`,
`ORCH-04`, `ORCH-08`, `TASK-08`, `CHAT-05`, `OPS-02` — статус `planned` в
каталоге, тестов ещё нет, вне зоны моей проверки в этом прогоне.

## Tier-2 (плейбуки уровня 2, набор post-deploy, kind: agent)

| Сценарий | Вердикт | Факты |
|---|---|---|
| SBX-01 | ✅ passed | Временная команда `E2E SBX-01 Probe` (тип `e2esbx01`) с одним sandbox-агентом `developer` (`code_backend=claude-code`, `provider_kind=anthropic_oauth`). Задача `c44a03e0` → `done` за ~1 минуту, MR #79 (файл `README.md`, строка `e2e: gh-32849605087-1`), ветка `task/c44a03e0-e2e-sbx-01-e2e-gh-32849605087-1-readme`. Проверено по GitHub API: PR реально открыт на этой ветке. |
| ORCH-01 | ✅ passed | Задача `4636cb31` (Development Team) → `done`, MR #80 (файл `ORCH_PROBE.md`), полный конвейер developer→(review/tester пропущены по правилам роутера для тривиальной правки, артефакт `code_diff` есть). |
| ORCH-01 (контроль: нечего коммитить) | ✅ passed | Отдельная задача `c5a5a94d` (Development Team), инструкция — не менять код, только прочитать README. Пришла в `needs_human` с причиной «команда объявила контракты артефактов (code_diff, delegation_result), но ни один не создан» — ровно ожидаемое поведение контрактного PR-гейта, не `done`. Ветка `task/c5a5a94d-e2e-orch-01-neg-readme` осталась без коммитов (SHA совпадает с `main`) — убрана. |
| TASK-04 | ✅ passed | На задаче `4636cb31` (уже `done` с MR #80) через REST `POST /tasks/:id/feedback` (mode=rework, `external_ref=task04-probe-gh-32849605087-1`) отправлено замечание — задача вернулась в `active` в ТОЙ ЖЕ ветке, затем снова в `done` («rework применён и одобрен ревьюером»), MR #80 не менялся (тот же номер). Повтор ТОГО ЖЕ запроса с тем же `external_ref` вернул `"duplicate":true` и тот же `feedback.id`/`rework_cycle:1` — второй круг доработки НЕ открылся. В отличие от прошлого прогона идемпотентность проверена: REST-путь `external_ref` использован явно (MCP-инструмент `task_feedback` этого поля не принимает). |
| TASK-07 | ✅ passed (с найденным багом платформы, см. ниже) | Задача-заказчик `949a2f27` (команда E2E) ушла в `awaiting_delegation`, породила дочернюю задачу `6e9249f6` в команде `E2E Delegatee` (`task_delegation_list` подтверждает связь `from_team=E2E → to_team=E2E Delegatee`, `status=failed` на записи делегирования). Дочерняя задача создала `code_diff` (коммит + PR #81 на ветке `task/6e9249f6-e2e-delegation-probe-gh-32849605087-1` — подтверждено по GitHub API как реально открытый), но сама пришла в `needs_human`: PR-гейт пытался открыть MR на другой ветке — `task.branch_name` (`task/6e9249f6-create-delegation-probe-md-in-e2e-repo-a`, авто-сгенерированной из заголовка, без единого коммита) — и получил 422 «No commits between main and …». Задача-заказчик корректно унаследовала `needs_human` от делегата и получила артефакт `delegation_result` (id `17fbbe1e…`, `state=needs_human`) — сам механизм делегирования (создание подзадачи, `task_delegation_list`, возврат `delegation_result`) отработал полностью штатно, поэтому по чек-листу плейбука сценарий `passed`; баг гейта — отдельная находка. |
| CHAT-02 | ✅ passed | Сессия ассистента `424c9245` (REST `/assistant/sessions`, поле сообщения — `content`, НЕ `message`; первая попытка с `message` вернула 400 `invalid input`). Сообщение с просьбой завести задачу → ассистент ПРЕДЛОЖИЛ `task_create` (team_id=E2E, внятные title/description/acceptance_criteria) и запарковался (`busy=true`, `pending_tool_call_id=call_784755`) — чекпоинт «предлагает, не создаёт молча» подтверждён. После `POST /confirm {approved:true}` (202) задача `000576e0` появилась в проекте команды E2E; поскольку у E2E нет своего developer-агента, она делегировалась `E2E Delegatee` (дочерняя `e22de61c`, MR #82, файл `CHAT02_PROBE.md`) и дошла до `done` с артефактом `delegation_result`. Сессия выдала финальный ответ («Задача успешно создана и сразу передана в работу…»), затем удалена (`DELETE /assistant/sessions/:id`, 204). |
| CHAT-03 | ⏭ skipped | `GET /projects/:id/scout` → `is_enabled: false`. Разведчик выключен в конфиге проекта — по плейбуку это `skipped`, не `failed`. Сессию ассистента для этого сценария не заводил (нет смысла без scout). |

## Наблюдения вне мандата (не чинил, только зафиксировал)

- **Найден дефект PR-гейта на делегированных подзадачах (TASK-07).** Дочерняя
  задача `6e9249f6` реально сделала работу — код закоммичен, PR #81 открыт и
  подтверждён живым на GitHub, — но гейт проверял `task.branch_name`
  (auto-сгенерированный из заголовка при создании делегированной подзадачи:
  `task/6e9249f6-create-delegation-probe-md-in-e2e-repo-a`), а не ветку, в
  которую агент реально закоммитил
  (`task/6e9249f6-e2e-delegation-probe-gh-32849605087-1`) — имена разошлись.
  Итог: задача честно сделана, но ушла в `needs_human` с ложной причиной «PR не
  открыт». В обычном (не делегированном) потоке SBX-01/ORCH-01 такого
  расхождения не было — ветка агента совпадала с `task.branch_name`. Похоже на
  специфику именно ветки делегированной подзадачи: она создаётся отдельно от
  ветки задачи-заказчика, и что-то в цепочке (воркер или PR-гейт) не
  синхронизирует итоговое имя. Не чинил — фиксирую для платформы.
- Ветки прошлых прогонов (`add-contributing-note`,
  `task/05e28b4c-…`, `task/08402eae-…`, `task/18b28c77-…`, `task/26dff38e-…`,
  `task/2d28491b-…`, `task/7205f072-…`, `task/89e8f372-…`, `task/a740ab90-…`,
  `task/d4742ba2-…`, `task/e5d99b89-…`) — не мной созданные, чужой мусор прошлых
  прогонов (тот же долг, что уже описан в `docs/e2e/README.md` §6). Не трогал.
  Ни одна из них не несёт `runs/` — при `git fetch` шаг 0 не нашёл отчётных
  веток для мержа (единственная ветка на сервере, кроме `main` и моей задачи, на
  старте прогона отсутствовала: `git branch -r` показал только `origin/main`,
  `git ls-remote --heads origin` в этот раз тоже не показал ни одной отчётной
  ветки — предыдущий прогон gh-32823241177-1 уже смержил всё, что было).

## Что требует человека

- **Дефект PR-гейта на ветках делегированных подзадач** (см. выше, TASK-07) —
  стоит завести как баг платформы: гейт должен проверять фактическую ветку
  агента, а не только `task.branch_name`, когда они могут разойтись.
- Остальное из выполненного мной не требует вмешательства человека — вердикты
  однозначны и ожидаемы.

## Непроверенное

- CHAT-03 не проверен по существу (скаут выключен) — только подтверждён факт
  `skipped`, поведение самого дозвона до scout_run не наблюдалось.
- Контроль «команда без `accepts_delegation` целью не выбирается» (часть
  чек-листа TASK-07) не гонял отдельно — не было времени/веской причины заводить
  ещё одну временную команду с `accepts_delegation=false` специально для этого;
  косвенно это уже проверено самим фактом, что делегирование ушло именно в
  `E2E Delegatee` (единственную команду с `accepts_delegation=true`).
- Сценарии, помеченные `planned` в каталоге и не входящие в tier-1 отчёт (список
  выше) — вне мандата уровня 2, поведение не проверялось.

## Уборка

Все заведённые сущности убраны:
- 4 пробные задачи первой волны (`c44a03e0`, `4636cb31`, `949a2f27` вместе с
  дочерней `6e9249f6`, `000576e0` вместе с дочерней `e22de61c`) и контрольная
  задача `c5a5a94d` — все удалены (`DELETE /tasks/:id`, 204 на каждой).
- Их ветки удалены (`git push origin --delete`): PR #79/#80/#81/#82
  автоматически закрыты GitHub при удалении веток (проверено `GET /pulls/{n}` →
  `state=closed`, `merged=false` на каждом); ветка контрольной задачи `c5a5a94d`
  без коммитов тоже удалена.
- Временная команда `E2E SBX-01 Probe` удалена (`team_delete`), временный тип
  команды `e2esbx01` удалён (`team_type_delete`).
- Сессия ассистента CHAT-02 удалена (`DELETE /assistant/sessions/:id`, 204).
- Расписания и вебхуки в этом прогоне не заводились.
