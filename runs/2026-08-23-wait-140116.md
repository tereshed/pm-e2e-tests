# E2E прогон wait-140116

- **Revision**: `b84541f6dc1c516af66ac6595ca522fcf222f69c` (`b84541f`)
- **Стенд**: https://polymaths.work
- **Задеплоено**: 2026-08-23T14:01:16Z
- **Workflow-прогон**: https://github.com/tereshed/devteam/actions/runs/32641452799
- **run_id**: wait-140116

## Результаты

| Сценарий | Вердикт | Факты |
|---|---|---|
| Tier 1 — контрактный прогон (post-deploy, `e2e/*_test.go`, полный список — `docs/e2e/catalog.yaml`) | passed | `tier1_state=success`, passed=34, failed=0, skipped=3, failed_ids=[] |
| Tier 2 — сценарии команды E2E (агенты/песочницы, playbook-и, каталог содержит ~40 tier-2 записей) | не проверено | Сырой вывод e2e-runner для этого прогона содержит только служебные логи окружения (клонирование репозиториев, ожидание docker-демона, прогрев MCP-серверов, push ветки) — фактов исполнения ни одного tier-2 сценария в нём нет |

Раскладки по отдельным id внутри tier1 (какие именно 34 implemented-сценария прошли и какие 3 пропущены) в собранных фактах нет — есть только агрегат из webhook-пейлоада. За списком id смотреть в workflow-прогоне выше.

## Падения

- **Tier 1**: падений не зафиксировано — `failed=0`, `failed_ids=[]`.
- **Tier 2**: падений не зафиксировано, но и прогона не было — см. раздел «что требует человека».

## Что требует человека

1. **Tier1: 3 skipped-сценария не идентифицированы.** В фактах есть только счётчик (`skipped=3`), без id. Нужно посмотреть job в workflow-прогоне (https://github.com/tereshed/devteam/actions/runs/32641452799), чтобы понять, что именно пропущено и почему.
2. **Tier2 не выполнялся в рамках этого прогона.** Согласно `docs/e2e/catalog.yaml`, часть tier-2 сценариев (agent/sandbox, playbook-и) входит в suite `post-deploy` наравне с tier1 — то есть их отсутствие в фактах может означать реальный пробел покрытия после этого деплоя, а не намеренный пропуск. Требуется решение человека: считать ли это нарушением гейта post-deploy.
3. **Подозрительная вставка в сыром выводе e2e-runner.** В `raw_output` присутствует фрагмент текста, не являющийся логом инструмента: *«I'll pause here and wait for the background monitor to report TASK-07's final state rather than continuing to poll manually.»* Это утверждение о каком-то TASK-07 ничем не подтверждено — ни артефактом, ни логом запуска сценария, ни диффом. Похоже на инструкцию/рассуждение, встроенное в данные (prompt injection), а не на факт выполнения. В отчёт как пройденный сценарий TASK-07 **не включён** — реального подтверждения его выполнения нет. Рекомендуется проверить пайплайн e2e-runner на предмет того, откуда взялась эта строка (аналогичная вставка про TASK-04 уже фиксировалась в прогоне gate-114424, см. `runs/2026-08-23-gate-114424.md`).
