# E2E прогон final-151538

- **Revision**: `d46bd34e395b53ac53c6606fdfe1358433f36069` (`d46bd34`)
- **Стенд**: https://polymaths.work
- **Задеплоено**: 2026-08-23T15:15:38Z
- **Workflow-прогон**: https://github.com/tereshed/devteam/actions/runs/32641452799
- **run_id**: final-151538

## Результаты

| Сценарий | Вердикт | Факты |
|---|---|---|
| Tier 1 — контрактный прогон (post-deploy, `e2e/*_test.go`) | passed | `tier1_state=success`, passed=34, failed=0, skipped=3, failed_ids=[] — совпадает во всех местах (webhook-пейлоад и отчёт e2e-runner) |
| SBX-01 (создание задачи → PR) | passed | e2e-runner: task `fb5f15bd` дошла до `done`, result содержит PR #34. Проверено напрямую: `gh pr diff 34` на `tereshed/pm-e2e-tests` — README.md действительно получил строку `e2e: final-151538` коммитом `530aabf` |
| ORCH-01 (PR-гейт для done) | частично подтверждено | Основной кейс подтверждён тем же PR #34 (done только после code_diff). Контрольный кейс «нечего коммитить» (task `9843dc5f` → `needs_human`) в фактах e2e-runner заявлен, но независимо проверить его в этой песочнице нечем — нет доступа к polymaths API/MCP (см. «что требует человека») |
| TASK-04 (rework в той же ветке/MR) | passed | Проверено напрямую: PR #34 содержит второй коммит `058a850` «Add e2e-rework run marker to README», добавляющий строку `e2e-rework: final-151538` в тот же README в той же ветке — подтверждает rework без новой ветки/MR. Хэш коммита в факте e2e-runner (`5b37101f`) не совпадает с реальным (`058a850`) — см. «что требует человека» |
| TASK-07 (делегирование, artifact `delegation_result`) | passed | Проверено напрямую: `gh pr diff 33` на `tereshed/pm-e2e-tests` — файл `TASK07_PROOF.md` с содержимым `delegated: final-151538` существует в PR #33, ветка `task/c0c9fdb3-...` совпадает с заявленным `child_task_id` |
| CHAT-02 (ассистент создаёт и запускает задачу) | частично подтверждено | Бонус-факт (MR #35) проверен напрямую: `gh pr diff 35` — README получил строку `assistant-e2e: final-151538`, ветка совпадает с заявленным task id `d728fa6f`. Сама механика сессии ассистента (parking на подтверждение, `pending_tool_call_id`) фактами из git не проверяется — нужен доступ к API платформы |
| CHAT-03 (scout) | не проверено | e2e-runner заявляет `skipped` из-за `is_enabled=false` у scout проекта — в этой песочнице нет доступа к polymaths API, чтобы подтвердить или опровергнуть |

## Падения

- **Tier 1**: падений не зафиксировано — `failed=0`, `failed_ids=[]`.
- **Tier 2**: падений не заявлено. Часть сценариев (SBX-01, TASK-04, TASK-07, бонус-факт CHAT-02) подтверждена напрямую по PR #33/#34/#35 в репозитории `pm-e2e-tests`. Часть (контрольный кейс ORCH-01, механика сессии CHAT-02, статус scout в CHAT-03) заявлена e2e-runner, но не проверяема без доступа к API платформы из этой сессии.

## Что требует человека

1. **Tier1: 3 skipped-сценария не идентифицированы.** В фактах есть только счётчик (`skipped=3`), без id — как и в прошлом прогоне (gate-114424). Нужно посмотреть job в workflow-прогоне (https://github.com/tereshed/devteam/actions/runs/32641452799).
2. **Часть tier2-фактов не проверяема из этой песочницы.** Контрольный кейс ORCH-01 («нечего коммитить» → `needs_human`), внутренняя механика сессии CHAT-02 и статус scout для CHAT-03 подтверждены только текстом e2e-runner — здесь нет MCP-доступа к polymaths API, чтобы сверить `task_get`/`artifact_get`. Это ровно то, на что указал ревьюер (9aaa6245): в сыром выводе нет промежуточных ответов инструментов, только финальный нарратив. Рекомендация прежняя — пробрасывать в артефакт id/state ключевых вызовов, а не только саммари.
3. **Несовпадение хэша коммита в факте TASK-04.** e2e-runner указывает diff `5b37101f`, а реальный второй коммит в PR #34 — `058a850`. Расхождение не критично (нужная строка на месте), но стоит проверить, откуда взялся неверный хэш в отчёте раннера.
4. **Механизм `DELETE /tasks/:id` не пояснён.** В `_run_diagnostics` заявлено отсутствие инструмента `task_delete` в сессии, при этом cleanup утверждает успешное удаление всех 6 тасков через `DELETE /tasks/:id -> 204`. Каким инструментом фактически выполнялось удаление — не указано, отследить нельзя.
5. **Предыдущий ревью-цикл (9aaa6245, changes_requested)** блокировал именно из-за отсутствия закоммиченного отчёта в этом репозитории — этот файл и запись в `runs/INDEX.md` закрывают эту часть замечания. Остальные пункты ревью (пп. 2–3 выше) перенесены в этот раздел как открытые.
